var ar=[5,64,45,34,6,54];

let result=ar.forEach(function(value,index){ 

    // We can get the values of array as well as their index from the function like map,filter,forEach
    console.log(`value in the array ${value}`)
    console.log(`index in the array ${index}`)
})
